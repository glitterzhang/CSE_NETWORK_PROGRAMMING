This program is for obtaining the file size of input file and calculating the number of matches of given string in  the input file  and output the result into outputfile.

Run this program:
1.Run Makefile.
2. Use  the command: count  <input-filename> < search-string> <output-filename>

*Before run the program, make sure that the texting file and count.c are under same directory.
